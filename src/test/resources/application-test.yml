spring:

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        show_sql: true

    redis:
      host: ${spring.redis.host}
      port: ${spring.redis.port}
